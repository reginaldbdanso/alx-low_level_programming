#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: integer value expected
 *
 * Return: -n if negative or n otherwise
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
