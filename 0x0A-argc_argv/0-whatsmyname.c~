#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *@argc - argument count
 *@argv - array of strings
 *Return: 0 Always (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
