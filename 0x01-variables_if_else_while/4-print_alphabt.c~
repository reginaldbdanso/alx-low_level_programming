#include <stdio.h>

/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
char lowerc;
lowerc = 'a';

while (lowerc <= 'z')
{
if (lowerc == 'e')
{
continue;
}
else if (lowerc == 'q')
{
continue;
}
else
{
putchar(lowerc);
lowerc++;
}
}

putchar('\n');

return (0);
}
